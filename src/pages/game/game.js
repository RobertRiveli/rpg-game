import { criarCard } from "./componentes/componentes.js";

const divContainer = document.querySelector(".secao-personagens");
criarCard(divContainer);
criarCard(divContainer);

criarCard(divContainer);
criarCard(divContainer);

criarCard(divContainer);
criarCard(divContainer);
criarCard(divContainer);
